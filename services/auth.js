const isLoggedIn = () => {
  return false;
};

const login = (email, password) => {
  console.log(email);
  console.log(password);
};

const register = (email, password) => {
  console.log(email);
  console.log(password);
};

export {
  isLoggedIn,
  login,
  register,
}